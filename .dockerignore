.env
.env.*
data/
backups/
.zhitian-restore-*/
.venv/
.git/
**/__pycache__/
*.pyc
.pytest_cache/
*.log
tests/
docs/design_reference/
# F37：嵌入模型由多阶段构建的导出阶段提供，本地开发副本不进构建上下文，
# 否则COPY . .会先写入一层约90MB、再被COPY --from覆盖，白白撑大镜像。
models/

# 与.gitignore的差集对齐：Dockerfile用的是`COPY . .`全量复制，凡.gitignore
# 排除而此处遗漏的本地工作产物都会进生产镜像。实测.workbuddy已进镜像（20文件
# 236K的审计HTML与工作笔记）；其余几项本机当前不存在，属"一旦创建即泄漏"的
# 潜在缺口，一并补上。注意`__pycache__`与`*.pyc`上方已覆盖，不重复。
.workbuddy/
临时信息库/
build/
.mypy_cache/
.ruff_cache/
*.bak-????-??-??
