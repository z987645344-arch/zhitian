# Starlette根治：0.49.1起的5条CVE（含CVE-2026-54283的urlencoded表单DoS）
# 在1.3.1才被官方修复，而fastapi<0.133.0声明starlette<0.50.0挡住升级路径，
# 故两者必须同步升。fastapi 0.133.0起去掉starlette上界，此处取双方最新。
fastapi==0.141.1
python-multipart==0.0.32
uvicorn==0.51.0
pydantic==2.13.4
# F31：LangGraph依赖组整体升级，消除langgraph/langchain-core/langsmith的
# 20条已知漏洞（OSV.dev实测：升级后这6个包均为0条）。
# langgraph 1.x起把checkpoint/prebuilt/sdk拆为独立包并强制安装，依赖面由3个
# 扩到6个，因此一并精确锁定，避免传递依赖自行漂移。其中langgraph-sdk必须
# >=0.3.15（CVE-2026-48776修复线）——0.1.1时代该包未安装、此前按错误映射
# 处理，升级后它成为真实安装包，这条约束开始生效。
langgraph==1.0.10
langchain-core==1.5.3
langsmith==0.10.15
langgraph-checkpoint==4.1.1
langgraph-prebuilt==1.0.13
langgraph-sdk==0.3.15
chromadb==0.5.0
# F32：chromadb 0.5.0 声明 numpy>=1.22.5 无上界，干净环境会解析到 NumPy 2.x，
# 而其代码使用 NumPy 2 已移除的 np.float_，import 即 AttributeError；pip check
# 因元数据合法而无法发现。1.26.4 是 1.x 最后一个版本，且满足 onnxruntime>=1.21.6
# 与 rank-bm25 的约束（openai 的 numpy>=2.0.2 仅属未安装的 voice-helpers extra）。
numpy==1.26.4
cryptography==48.0.1
tavily-python==0.3.3
python-dotenv==1.2.2
sniffio==1.3.1
mcp==1.28.1
starlette==1.4.1
sse-starlette==3.0.3
PyJWT==2.13.0
pdfplumber==0.11.10
# F43：CVE-2026-71852/71870（文本提取时字体/CMap解析的资源耗尽DoS）修复版。
# 项目当前代码路径不可达（只用PdfReader/PdfWriter做合并拆分，文本提取走
# pdfplumber→pdfminer.six），升级是为防止将来给pdf_tools加文本提取时该保护悄然失效。
pypdf==6.15.0
python-docx==1.2.0
openpyxl==3.1.5
python-pptx==1.0.2
PyMuPDF==1.28.0
bcrypt==5.0.0
slowapi==0.1.10
rank_bm25==0.2.2
pytest==9.1.1
openai==2.47.0
alibabacloud_dm20151123==1.11.0
